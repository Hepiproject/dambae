{
  "name": "dambae-ait",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "담배 한 대 - 앱인토스 미니앱 번들 래퍼",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "dev": "node scripts/build-dist.mjs"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^2.10.7"
  },
  "//devDependencies": "ait build 의 collectDependencyVersions 가 Object.keys(devDependencies) 를 무방비로 호출한다. 비어 있어도 키 자체는 있어야 한다.",
  "devDependencies": {}
}
